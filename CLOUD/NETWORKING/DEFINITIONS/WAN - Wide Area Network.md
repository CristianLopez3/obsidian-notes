Crazy notes

* Two local area networks that are separate.
* WAN allows to connect two routers that are in two different geographical areas.
* A static route is something that is manually settled.
* Dynamic routing protocol, it's where each router learns how the other router works.

* Wide Area - Large geographical distance
* We can use static routes or dynamic routes
* Allows communication between large distances with wires or over routers.